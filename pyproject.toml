[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "averis-email"
version = "0.1.0"
description = "Starter pipeline for email document comparison"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"]
